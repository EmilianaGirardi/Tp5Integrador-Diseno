server:
  port: 8080
spring:
  application:
    name: Command
  jpa:
    hibernate:
      ddl-auto: none  # Opcional, si no quieres que Spring intente manejar el esquema de la BD
  kafka:
    producer:
      bootstrap-servers: localhost:9093  # Dirección del servidor Kafka
    template:
      default-topic: eventos
